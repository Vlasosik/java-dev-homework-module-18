package com.example.service.registration_and_login_service.response.enum_error;

public enum ErrorMessage {
    OK,
    USER_ALREADY_EXISTS,
    INVALID_LOGIN,
    INVALID_PASSWORD,
    USER_DOES_NOT_EXIST
}
